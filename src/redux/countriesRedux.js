/* selectors */

export const getCountries = ({ countries }) => countries;

export const reducer = (statePart = [], action = {}) => {
  switch (action.type) {
    default:
      return statePart;
  }
};
